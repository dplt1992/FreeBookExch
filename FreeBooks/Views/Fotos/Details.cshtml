@model FreeBooks.Models.Fotos

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Fotos</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Foto)
        </dt>
        <dd class = "col-sm-10">
           @*@Html.DisplayFor(model => model.Foto)*@ 
            <img src="~/Fotos/@Model.Foto"
                 alt="Foto of @Model.Foto"
                 title="@Model.Foto"
                 height="100" />
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Livros)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Livros.IdLivro)
        </dd>
    </dl>
</div>
@*
<table>
    @foreach (var livro in Model.Livros)
    {
        <h4>
            <a asp-area="" asp-controller="Livros" asp-action="Index"  asp-route-id="@livro.IdLivro" >
                @livro.IdLivro => @livro.Titulo
            </a>
        </h4>
    }
</table>
*@
<div>
    <a asp-action="Edit" asp-route-id="@Model?.IdFoto">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
