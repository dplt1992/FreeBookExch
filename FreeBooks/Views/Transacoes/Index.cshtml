@model IEnumerable<FreeBooks.Models.Transacoes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataTrasancao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataTrasancao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTransacao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTransacao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTransacao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
